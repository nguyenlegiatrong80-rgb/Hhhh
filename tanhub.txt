--// TẦN HUB (FULL + SUBTITLE)

local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "TanHub"

-- SAVE STATE
local toggles = {}

-- MAIN
local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 520, 0, 360)
main.Position = UDim2.new(0.5, -260, 0.5, -180)
main.BackgroundColor3 = Color3.fromRGB(18,18,18)
main.Active = true
main.Draggable = true

-- OPEN ANIMATION
main.Size = UDim2.new(0,0,0,0)
main:TweenSize(UDim2.new(0,520,0,360), "Out", "Quad", 0.4, true)

-- TITLE
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1,0,0,40)
title.Text = "TẦN HUB"
title.TextColor3 = Color3.fromRGB(200,0,255)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 22

-- TITLE GLOW
local stroke = Instance.new("UIStroke", title)
stroke.Color = Color3.fromRGB(170,0,255)
stroke.Thickness = 1.5

-- SUBTITLE
local subtitle = Instance.new("TextLabel", main)
subtitle.Size = UDim2.new(1,0,0,20)
subtitle.Position = UDim2.new(0,0,0,30)
subtitle.Text = "By real_GT"
subtitle.TextColor3 = Color3.fromRGB(150,150,150)
subtitle.BackgroundTransparency = 1
subtitle.Font = Enum.Font.Gotham
subtitle.TextSize = 14

-- SUBTITLE GLOW
local stroke2 = Instance.new("UIStroke", subtitle)
stroke2.Color = Color3.fromRGB(120,0,180)
stroke2.Thickness = 1

-- LOGO (THAY ID ẢNH)
local logo = Instance.new("ImageLabel", main)
logo.Size = UDim2.new(0,40,0,40)
logo.Position = UDim2.new(1,-45,0,0)
logo.BackgroundTransparency = 1
logo.Image = "rbxassetid://PUT_YOUR_IMAGE_ID"

-- SIDEBAR
local sidebar = Instance.new("Frame", main)
sidebar.Size = UDim2.new(0,130,1,-40)
sidebar.Position = UDim2.new(0,0,0,40)
sidebar.BackgroundColor3 = Color3.fromRGB(25,25,25)

local UIList = Instance.new("UIListLayout", sidebar)

-- CONTENT
local content = Instance.new("Frame", main)
content.Size = UDim2.new(1,-130,1,-40)
content.Position = UDim2.new(0,130,0,40)
content.BackgroundColor3 = Color3.fromRGB(20,20,20)

-- TAB FUNCTION
function createTab(name)
    local btn = Instance.new("TextButton", sidebar)
    btn.Size = UDim2.new(1,0,0,45)
    btn.Text = name
    btn.BackgroundColor3 = Color3.fromRGB(35,35,35)
    btn.TextColor3 = Color3.new(1,1,1)

    local frame = Instance.new("Frame", content)
    frame.Size = UDim2.new(1,0,1,0)
    frame.Visible = false
    frame.BackgroundTransparency = 1

    local layout = Instance.new("UIListLayout", frame)
    layout.Padding = UDim.new(0,10)

    btn.MouseButton1Click:Connect(function()
        for _,v in pairs(content:GetChildren()) do
            if v:IsA("Frame") then v.Visible = false end
        end
        frame.Visible = true
    end)

    return frame
end

-- TOGGLE FUNCTION
function createToggle(parent, name)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0,220,0,40)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.BackgroundColor3 = Color3.fromRGB(50,50,50)

    toggles[name] = toggles[name] or false

    local function update()
        if toggles[name] then
            btn.Text = name .. ": ON"
            btn.BackgroundColor3 = Color3.fromRGB(170,0,255)
        else
            btn.Text = name .. ": OFF"
            btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
        end
    end

    update()

    btn.MouseButton1Click:Connect(function()
        toggles[name] = not toggles[name]
        update()
    end)
end

-- TABS
local farm = createTab("Farm")
local quest = createTab("Quest")
local misc = createTab("Misc")

farm.Visible = true

-- TOGGLES
createToggle(farm, "Auto Farm Level")
createToggle(farm, "Auto Farm Nearest")

createToggle(quest, "Auto Quest")
createToggle(quest, "Auto Boss")

createToggle(misc, "Auto Travel")
createToggle(misc, "Auto Raid")

-- CLOSE BUTTON
local close = Instance.new("TextButton", main)
close.Size = UDim2.new(0,40,0,40)
close.Position = UDim2.new(1,-85,0,0)
close.Text = "X"
close.BackgroundColor3 = Color3.fromRGB(80,0,0)

-- MINI OPEN BUTTON
local openBtn = Instance.new("TextButton", gui)
openBtn.Size = UDim2.new(0,50,0,50)
openBtn.Position = UDim2.new(0,20,0.5,-25)
openBtn.Text = "≡"
openBtn.BackgroundColor3 = Color3.fromRGB(170,0,255)
openBtn.TextColor3 = Color3.new(1,1,1)
openBtn.Visible = false

-- CLOSE LOGIC
close.MouseButton1Click:Connect(function()
    main.Visible = false
    openBtn.Visible = true
end)

-- OPEN LOGIC
openBtn.MouseButton1Click:Connect(function()
    main.Visible = true
    openBtn.Visible = false

    main.Size = UDim2.new(0,0,0,0)
    main:TweenSize(UDim2.new(0,520,0,360), "Out", "Quad", 0.3, true)
end)